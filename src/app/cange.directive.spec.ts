import { CangeDirective } from './cange.directive';

describe('CangeDirective', () => {
  it('should create an instance', () => {
    const directive = new CangeDirective();
    expect(directive).toBeTruthy();
  });
});
